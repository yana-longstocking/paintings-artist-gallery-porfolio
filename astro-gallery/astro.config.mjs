// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: 'https://yana-longstocking.github.io/',
  base: '/paintings-artist-gallery-porfolio'
});
